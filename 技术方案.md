---
aliases: 
tags: 
title: 二、整体架构
date created: 2026-08-27 22:47:46
date modified: 2026-08-27 22:49:28
---

一个独立的 Windows 托盘工具，例如项目名：

**PinyinSwitcher**

核心目标：

> 常驻 Windows 10 任务栏通知区域，右键托盘图标即可在微软拼音“全拼 / 双拼”之间切换，并提供快捷键、状态提示和开机启动。

微软拼音目前可以通过当前用户注册表中的 `Enable Double Pinyin` 判断全拼/双拼状态：`0` 为全拼、`1` 为双拼。这个位置在 Microsoft Q&A 中有实际验证记录。([Microsoft Learn](https://learn.microsoft.com/zh-cn/answers/questions/3212757/question-3212757?utm_source=chatgpt.com "微软拼音输入法，能否支持快速切换全拼和双拼，或者全拼和双拼混用？ - Microsoft Q&A"))

## 一、技术选型

我建议：

|技术|选择|
|---|---|
|开发语言|C#|
|UI|WPF|
|目标系统|Windows 10 x64|
|Framework|**.NET Framework 4.8**|
|托盘|`System.Windows.Forms.NotifyIcon`|
|配置存储|JSON / Registry|
|输入法配置|Windows Registry|
|系统交互|Win32 API|
|日志|简单文本日志即可|
|安装方式|单文件/安装包均可|

这里我反而**不建议这个项目直接上 .NET 10**。

目前 .NET 10 是 LTS，到 2028 年，但微软现在对现代 .NET 的 Windows 10 官方支持已经有限制；而这个工具的核心目标恰恰是 Windows 10。([Microsoft](https://dotnet.microsoft.com/en-us/platform/support/policy?utm_source=chatgpt.com "The official .NET support policy | .NET"))

所以对于这种纯 Windows 小工具：

> **WPF + .NET Framework 4.8 是更保守、更适合 Win10 的方案。**

而且不需要引入复杂第三方 UI 框架。

托盘功能直接用 Windows Forms 的 `NotifyIcon` 即可，WPF 项目引用 `System.Windows.Forms`。微软官方支持 NotifyIcon 的托盘图标、Tooltip 和右键菜单。([Microsoft Learn](https://learn.microsoft.com/en-us/dotnet/desktop/winforms/controls/notifyicon-component-windows-forms?utm_source=chatgpt.com "NotifyIcon Component - Windows Forms | Microsoft Learn"))

---

# 二、整体架构

建议不要把注册表操作、托盘操作全部写到 `MainWindow.xaml.cs`。

拆成下面几层：

```text
PinyinSwitcher
│
├── App.xaml
├── App.xaml.cs
│
├── Views
│   └── SettingsWindow.xaml
│
├── ViewModels
│   └── SettingsViewModel.cs
│
├── Services
│   ├── TrayService.cs
│   ├── PinyinService.cs
│   ├── RegistryService.cs
│   ├── ImeRefreshService.cs
│   ├── HotkeyService.cs
│   ├── StartupService.cs
│   ├── NotificationService.cs
│   └── ConfigService.cs
│
├── Models
│   ├── PinyinMode.cs
│   └── AppConfig.cs
│
├── Native
│   └── NativeMethods.cs
│
├── Resources
│   ├── app.ico
│   ├── full.ico
│   └── double.ico
│
└── Utils
    └── Logger.cs
```

整体调用关系：

```text
                ┌──────────────────┐
                │   TrayService    │
                │    托盘菜单       │
                └────────┬─────────┘
                         │
                         ▼
                ┌──────────────────┐
                │  PinyinService   │
                │   拼音业务层      │
                └───────┬──────────┘
                        │
             ┌──────────┴──────────┐
             ▼                     ▼
    ┌─────────────────┐   ┌──────────────────┐
    │ RegistryService │   │ImeRefreshService │
    │ 修改微软拼音配置 │   │  通知IME重新加载 │
    └─────────────────┘   └──────────────────┘
```

---

# 三、核心功能

第一版我建议只做 6 个功能。

### 1. 托盘常驻

软件启动以后：

```text
不显示主窗口
      ↓
直接进入托盘
```

托盘图标：

```text
Ⓟ
```

鼠标悬停：

```text
Pinyin Switcher - 当前：双拼
```

`NotifyIcon` 就是为这种后台驻留程序设计的。([Microsoft Learn](https://learn.microsoft.com/en-us/dotnet/desktop/winforms/controls/notifyicon-component-overview-windows-forms?utm_source=chatgpt.com "NotifyIcon Component Overview - Windows Forms | Microsoft Learn"))

---

# 四、托盘菜单设计

右键：

```text
Pinyin Switcher
────────────────
● 全拼
○ 双拼
────────────────
  快速切换       Ctrl + Alt + P
────────────────
  设置
  开机启动
────────────────
  退出
```

例如当前是双拼：

```text
○ 全拼
● 双拼
```

点击：

```text
全拼
```

执行：

```text
读取当前状态
   ↓
写入注册表
   ↓
通知微软拼音刷新
   ↓
重新读取状态
   ↓
更新托盘菜单
   ↓
显示通知

「已切换至：全拼」
```

---

# 五、微软拼音配置

核心注册表路径：

```text
HKEY_CURRENT_USER
\Software
\Microsoft
\InputMethod
\Settings
\CHS
```

核心值：

```text
Enable Double Pinyin
```

状态定义：

```csharp
public enum PinyinMode
{
    FullPinyin,
    DoublePinyin
}
```

映射：

```text
Enable Double Pinyin = 0
→ 全拼

Enable Double Pinyin = 1
→ 双拼
```

微软 Q&A 上记录的也是这个配置。([Microsoft Learn](https://learn.microsoft.com/zh-cn/answers/questions/3212757/question-3212757?utm_source=chatgpt.com "微软拼音输入法，能否支持快速切换全拼和双拼，或者全拼和双拼混用？ - Microsoft Q&A"))

因此 `PinyinService` 可以设计成：

```text
PinyinService
│
├── GetCurrentMode()
│
├── SetMode(PinyinMode mode)
│
├── Toggle()
│
├── IsMicrosoftPinyinInstalled()
│
└── Refresh()
```

例如：

```csharp
public interface IPinyinService
{
    PinyinMode GetCurrentMode();

    bool SetMode(PinyinMode mode);

    PinyinMode Toggle();
}
```

---

# 六、最重要的问题：IME 刷新

这里才是整个项目最值得先做验证的地方。

单纯：

```text
Registry.SetValue(...)
```

并不能保证：

```text
正在运行的微软拼音
```

立刻读取新的设置。

原因是微软拼音进程可能已经把配置缓存到内存。

所以我建议设计三级刷新策略。

### Level 1：广播系统配置变化

调用 Win32：

```text
SendMessageTimeout
```

广播：

```text
WM_SETTINGCHANGE
```

即：

```text
修改 Registry
      ↓
WM_SETTINGCHANGE
      ↓
通知系统设置发生变化
```

这是最温和的方案。

---

### Level 2：输入法重新激活

如果 Level 1 不够，就：

```text
获取当前输入法
      ↓
临时切换输入上下文
      ↓
重新激活 Microsoft Pinyin
```

这里可以研究：

```text
ActivateKeyboardLayout
LoadKeyboardLayout
ImmGetContext
ImmReleaseContext
```

以及 TSF。

但要注意：

> Windows 10 微软拼音实际上已经大量基于 TSF，而不是传统 IMM32，所以不能假设 `ImmNotifyIME()` 就一定解决问题。

因此这部分必须做实际机器测试。

---

### Level 3：IME 进程刷新

如果前两种仍然无法即时应用，可以增加 fallback。

例如检测：

```text
ChsIME.exe
```

微软拼音相关进程。

必要时让 IME 自身重新初始化。

但我建议：

> **不要第一版就直接 Kill 输入法进程。**

因为可能造成：

```text
输入中断
候选框消失
正在输入文字丢失
某些机器自动恢复异常
```

这个只能作为最终 fallback。

---

# 七、建议专门做一个“兼容策略”

不要在 `PinyinService` 里面写：

```csharp
Registry.SetValue();
KillProcess();
```

这种代码。

而是：

```text
ImeRefreshService

Refresh()
  │
  ├─ Strategy A
  │   WM_SETTINGCHANGE
  │
  ├─ Strategy B
  │   Reload Input Context
  │
  └─ Strategy C
      Restart IME
```

以后发现 Windows 11 行为不同，只改这里。

---

# 八、快捷键

增加一个全局快捷键：

```text
Ctrl + Alt + P
```

调用 Win32：

```text
RegisterHotKey
UnregisterHotKey
```

执行：

```text
Ctrl + Alt + P
        ↓
PinyinService.Toggle()
        ↓
全拼 → 双拼

或者

双拼 → 全拼
```

然后显示：

```text
Pinyin Switcher

已切换至：
双拼
```

这样日常使用甚至不需要打开托盘菜单。

---

# 九、双拼方案不要跟“全拼/双拼”混在一起

这个地方建议提前设计。

微软拼音里面其实存在两个概念：

```text
① 当前使用全拼还是双拼

② 双拼使用什么方案
```

例如：

```text
微软双拼
自然码
小鹤双拼
智能ABC
紫光
自定义
```

所以数据模型应该分开：

```csharp
PinyinMode
{
    Full,
    Double
}
```

以及：

```csharp
DoublePinyinScheme
{
    Microsoft,
    Ziranma,
    Xiaohe,
    ABC,
    Custom
}
```

第一版可以**只控制 Full / Double**。

不要修改用户已经配置好的双拼方案。

比如用户自己设置的是：

```text
小鹤双拼
```

程序切：

```text
双拼 → 全拼 → 双拼
```

以后回来仍然应该是：

```text
小鹤双拼
```

而不是被你的程序改成微软双拼。

这是一个比较重要的设计原则。

---

# 十、设置窗口

虽然主程序是托盘程序，我还是建议保留一个非常简单的 WPF 设置窗口。

例如：

```text
┌─────────────────────────────────┐
│ Pinyin Switcher                 │
├─────────────────────────────────┤
│                                 │
│ 当前输入模式                    │
│                                 │
│  ○ 全拼      ● 双拼             │
│                                 │
│ ─────────────────────────────   │
│                                 │
│ 快捷键                          │
│                                 │
│ Ctrl + Alt + P                  │
│                                 │
│ ☑ 开机自动启动                  │
│ ☑ 切换后显示通知                │
│ ☑ 软件启动后最小化到托盘        │
│                                 │
│                保存             │
└─────────────────────────────────┘
```

不要做复杂 UI。

这个工具的价值是：

```text
启动快
占用低
稳定
一键切换
```

而不是界面漂亮。

---

# 十一、配置文件

软件自己的设置不要塞进微软输入法 Registry。

例如：

```text
%AppData%\PinyinSwitcher\config.json
```

内容：

```json
{
  "hotkey": "Ctrl+Alt+P",
  "startWithWindows": true,
  "showNotification": true,
  "startMinimized": true
}
```

微软拼音模式：

```text
不要自己保存
```

每次直接读取系统：

```text
Registry
```

这样不会出现：

```text
程序显示“双拼”
但是 Windows 实际是“全拼”
```

---

# 十二、开机启动

推荐：

```text
HKCU\Software\Microsoft\Windows\CurrentVersion\Run
```

例如：

```text
PinyinSwitcher
=
"C:\Program Files\PinyinSwitcher\PinyinSwitcher.exe"
```

因为写的是：

```text
HKCU
```

因此正常情况下：

> **不需要管理员权限。**

包括微软拼音配置也是：

```text
HKEY_CURRENT_USER
```

所以整个软件原则上：

```text
不申请管理员权限
```

这是正确设计。

---

# 十三、单实例运行

必须加。

否则用户点两次 EXE：

```text
PinyinSwitcher.exe
PinyinSwitcher.exe
```

就会出现：

```text
两个托盘图标
两个全局快捷键
两个状态监听
```

使用：

```csharp
Mutex
```

控制：

```text
PinyinSwitcher
        ↓
Mutex
        ↓
是否已经运行？
    ├── 是 → 退出
    └── 否 → 正常启动
```

---

# 十四、错误处理

至少考虑这些情况：

```text
微软拼音不存在

Registry Key 不存在

Enable Double Pinyin 不存在

注册表写入失败

快捷键被其他软件占用

IME刷新失败

Explorer重启导致托盘图标丢失
```

程序不能 Crash。

例如：

```text
检测不到微软拼音

→ 托盘菜单：
  全拼（禁用）
  双拼（禁用）

→ 提示：
  未检测到 Microsoft Pinyin
```

---

# 十五、日志

不需要 Serilog 这种完整框架也可以。

简单：

```text
%LocalAppData%
\PinyinSwitcher
\logs
\2026-08-27.log
```

例如：

```text
22:10:01 App started
22:10:01 Microsoft Pinyin detected
22:10:01 Current mode: DoublePinyin

22:15:32 Toggle requested
22:15:32 Registry updated: 1 -> 0
22:15:32 IME refresh success
22:15:32 Current mode: FullPinyin
```

后面调 Win10 不同版本兼容问题的时候非常有价值。

---

# 十六、启动流程

最终启动流程应该是：

```text
PinyinSwitcher.exe
        │
        ▼
检查单实例
        │
        ▼
读取程序配置
        │
        ▼
检测 Microsoft Pinyin
        │
        ▼
读取 Enable Double Pinyin
        │
        ▼
初始化 Tray Icon
        │
        ▼
注册全局快捷键
        │
        ▼
后台运行
```

软件启动后默认：

```text
不出现窗口
```

直接：

```text
托盘驻留
```

---

# 十七、切换流程

这是整个项目最核心的一条链：

```text
用户点击「全拼」
        │
        ▼
PinyinService.SetMode(Full)
        │
        ▼
读取 Registry 当前状态
        │
        ├── 已经是 Full
        │       ↓
        │     不操作
        │
        ▼
RegistryService.Write(0)
        │
        ▼
ImeRefreshService.Refresh()
        │
        ▼
重新读取 Registry
        │
        ▼
更新 Tray 状态
        │
        ▼
显示通知
        │
        ▼
已切换到「全拼」
```

---

# 十八、第一版不要做这些

我建议 V1 暂时不要加入：

```text
× Windows 11 专门适配

× 搜狗输入法

× QQ输入法

× 小狼毫

× 云同步

× 自动更新

× DLL Hook

× ctfmon 注入

× TSF 自定义 Text Service

× 修改系统语言栏菜单
```

这些都会把一个：

```text
1 天级别小工具
```

搞成：

```text
长期维护项目
```

你的需求其实不需要。

---

# 十九、V1 最终功能范围

我会把第一版定死为：

```text
PinyinSwitcher V1

[核心]

√ Win10
√ Microsoft Pinyin
√ 全拼
√ 双拼

[操作]

√ 托盘右键切换
√ Ctrl + Alt + P 快速切换

[体验]

√ 当前模式打勾
√ 切换通知
√ 开机启动
√ 单实例

[系统]

√ 无管理员权限
√ 自动检测微软拼音
√ 错误日志
```

整体代码量不会很大。

---

# 二十、我建议先做一个 Spike

正式写 UI 之前，**第一件事情不是创建 WPF 页面**。

先做一个非常小的控制台验证程序：

```text
PinyinSpike.exe

1. 当前状态
2. 切换全拼
3. 切换双拼
4. 刷新 IME
5. 退出
```

重点验证：

```text
Registry 修改
          +
IME Refresh
          ↓
微软拼音是否立即生效
```

例如测试：

```text
当前：双拼

输入：
nihao

切换全拼

立即输入：
nihao

是否得到：
你好
```

只有这个实验验证成功以后，再开始写：

```text
WPF
Tray
Settings
Hotkey
Startup
```

**因为这个项目 80% 的技术风险都集中在“微软拼音是否即时刷新”这一点。**

---

## 最终架构我建议定成这样

```text
             PinyinSwitcher
                   │
        ┌──────────┴──────────┐
        │                     │
        ▼                     ▼
   WPF Settings          TrayService
                              │
                        HotkeyService
                              │
                              ▼
                       PinyinService
                              │
              ┌───────────────┴──────────────┐
              │                              │
              ▼                              ▼
       RegistryService               ImeRefreshService
              │                              │
              ▼                         Win32 / TSF
 Microsoft Pinyin Registry
              │
              ▼
      Enable Double Pinyin
          0         1
          │         │
         全拼       双拼
```

如果是我来落地，我会采用 **`.NET Framework 4.8 + WPF + System.Windows.Forms.NotifyIcon + Registry + Win32 RegisterHotKey`** 这一套，不引入第三方依赖。

下一步最合适的是先把 **`PinyinSpike` 验证程序做出来**，验证“修改注册表后如何做到立即切换”。验证通过后，再把这部分封装成 `PinyinService` 接入 WPF，后面的工作基本就是常规桌面开发。